import React from "react";

const TextError = (props) => {
  return (
    <div className="" style={{ color: "#F64E60", paddingTop: "5px" }}>
      {props.children}
    </div>
  );
};

export default TextError;
